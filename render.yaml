services:
  - type: worker
    name: telegram-feedback-bot
    env: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: |
      python -m bot
    plan: free
